import { useEffect } from "react";
import useCategoryHook from "../../hooks/useCategoryHook";
const AddCategory = () => {
  const { getAllCategories } = useCategoryHook();
  // getAllCategories("?limit=8");
  useEffect((
    g 
  ) => {});
  return <div></div>;
};

export default AddCategory;
